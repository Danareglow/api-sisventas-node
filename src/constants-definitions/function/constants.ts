// Definimos un enumerador (enum) llamado "Collection" que enumera los nombres de colecciones.
export enum Collection {
  USUARIOS = "usuarios",
  CATEGORIAS = "categorias",
  PRODUCTOS = "productos",
  VENTAS = "ventas",
}

export enum MetodosPeticion {
  POST = "POST",
  PUT = "PUT",
  DELETE = "DELETE",
  GET = "GET",
}
